<%
  function _diasPlanoLabelServico(diasStr) {
    var DIAS = ['dom', 'seg', 'ter', 'qua', 'qui', 'sex', 'sáb'];
    var dias = String(diasStr || '').split(',').map(Number).filter(function (n) { return !isNaN(n); }).sort(function (a, b) { return a - b; });
    if (dias.length === 7) return 'todos os dias';
    var corrido = dias.every(function (d, i) { return i === 0 || d === dias[i - 1] + 1; });
    if (corrido && dias.length > 1) return DIAS[dias[0]] + '–' + DIAS[dias[dias.length - 1]];
    return dias.map(function (d) { return DIAS[d]; }).join(', ');
  }
%>

<%/* TURNO 6 (2026-07-28): a grade de dois cartões por linha virou uma LISTA
     separada por fios, com o nome do serviço a 34px — é a peça central da
     tela de escolha na referência. O selecionado inverte (bloco preto), em
     vez de ganhar borda/selo de check. */%>
<div class="ag6-rolagem">
  <div class="ag6-cabeca">
    <div class="ag6-titulo">Serviços</div>
    <% if (!assinatura) { %>
      <a href="/agendar/plano" class="ag6-selo">Já tenho um plano</a>
    <% } %>
  </div>

  <% if (assinatura) { %>
    <div class="ag6-aviso">
      Usando o plano <strong><%= assinatura.plano.nome %></strong><%= assinatura.usosRestantes !== null ? ' · ' + assinatura.usosRestantes + ' uso(s) restante(s)' : ' · ilimitado (' + _diasPlanoLabelServico(assinatura.plano.diasSemana) + ')' %>.
      <a href="/agendar">Agendar sem plano</a>
    </div>
  <% } %>

  <% if (servicos.length === 0) { %>
    <p class="ag6-vazio">Nenhum serviço disponível no momento.</p>
  <% } else if (assinatura) { %>
    <%/* Plano: seleção única e direta por link — o plano cobre um serviço por
         agendamento, então não há o que somar nem barra de total. */%>
    <div class="ag6-sobrancelha-linha">
      <span class="ag6-sobrancelha">Escolha o serviço</span>
    </div>
    <div class="ag6-lista">
      <% servicos.forEach(function (s, i) { %>
        <a class="ag6-servico" href="/agendar/barbeiro?servicoIds=<%= s.id %>&assinatura=<%= assinatura.id %>" style="animation-delay:<%= (0.2 + i * 0.07).toFixed(2) %>s">
          <span class="ag6-servico-txt">
            <span class="ag6-servico-nome"><%= s.nome %></span>
            <span class="ag6-servico-meta"><%= s.duracaoMin %> min · Incluso</span>
          </span>
          <% if (s.fotoUrl) { %><span class="ag6-servico-foto"><img src="<%= s.fotoUrl %>" alt="" /></span><% } %>
        </a>
      <% }) %>
      <div class="ag6-lista-fim"></div>
    </div>
  <% } else { %>
    <form id="form-servicos" action="/agendar/barbeiro" method="GET" onsubmit="return validarSelecao()">
      <input type="hidden" name="servicoIds" id="servicoIds-input" value="" />
      <div class="ag6-sobrancelha-linha">
        <span class="ag6-sobrancelha">Escolha os serviços</span>
        <span class="ag6-sobrancelha-val" id="ag6-conta">toque para escolher</span>
      </div>
      <div class="ag6-lista" id="lista-servicos">
        <% servicos.forEach(function (s, i) { %>
          <button type="button" class="ag6-servico" data-id="<%= s.id %>" data-valor="<%= s.valor %>" data-min="<%= s.duracaoMin %>" onclick="toggleServico(this)" style="animation-delay:<%= (0.2 + i * 0.07).toFixed(2) %>s">
            <span class="ag6-servico-txt">
              <span class="ag6-servico-nome"><%= s.nome %></span>
              <span class="ag6-servico-meta"><%= s.duracaoMin %> min · <%= fmtT6(s.valor) %></span>
            </span>
            <% if (s.fotoUrl) { %><span class="ag6-servico-foto"><img src="<%= s.fotoUrl %>" alt="" /></span><% } %>
          </button>
        <% }) %>
        <div class="ag6-lista-fim"></div>
      </div>
    </form>
  <% } %>
</div>

<% if (!assinatura && servicos.length > 0) { %>
  <%/* A barra fica SEMPRE visível (a referência esconde enquanto nada está
       escolhido). Aqui ela é o próprio submit do formulário, então some-la
       tiraria a única pista de "e agora?" de quem abriu a tela. Sem seleção,
       ela usa o par de cores inerte fio/apoio. */%>
  <div class="ag6-barra">
    <button class="ag6-acao ag6-inerte" type="submit" form="form-servicos" id="btn-continuar" disabled>
      <span>
        <span class="ag6-acao-rot" id="ag6-cta-rot">Escolha um serviço</span>
        <span class="ag6-acao-sub" id="ag6-cta-sub">para seguir pro barbeiro</span>
      </span>
      <span class="ag6-acao-total" id="ag6-cta-total">R$0</span>
    </button>
  </div>

  <script>
    // Seleção múltipla no cliente: o formulário leva só a lista de ids em um
    // campo escondido, então tudo o que a barra mostra (contagem, duração
    // somada, total) é recalculado aqui a cada toque.
    const selecionados = new Set();
    const precos = {};
    const duracoes = {};
    document.querySelectorAll('#lista-servicos .ag6-servico').forEach(function (el) {
      precos[el.dataset.id] = Number(el.dataset.valor);
      duracoes[el.dataset.id] = Number(el.dataset.min);
    });
    // Formato do Turno 6: "R$40" — sem centavos e sem espaço, porque o total
    // aparece a 22px na barra e ",00" só rouba largura.
    function fmtT6Local(centavos) {
      return 'R$' + Math.round(centavos / 100).toLocaleString('pt-BR');
    }
    function fmtDuracaoLocal(min) {
      if (min < 60) return min + ' min';
      const h = Math.floor(min / 60);
      const r = min % 60;
      return h + 'h' + (r ? String(r).padStart(2, '0') : '');
    }
    function toggleServico(el) {
      const id = el.dataset.id;
      if (selecionados.has(id)) {
        selecionados.delete(id);
        el.classList.remove('sel');
      } else {
        selecionados.add(id);
        el.classList.add('sel');
      }
      document.getElementById('servicoIds-input').value = Array.from(selecionados).join(',');

      const n = selecionados.size;
      const btn = document.getElementById('btn-continuar');
      btn.disabled = n === 0;
      btn.classList.toggle('ag6-inerte', n === 0);
      document.getElementById('ag6-conta').textContent = n ? n + ' selecionado' + (n > 1 ? 's' : '') : 'toque para escolher';

      const ids = Array.from(selecionados);
      const total = ids.reduce(function (s, id) { return s + precos[id]; }, 0);
      const min = ids.reduce(function (s, id) { return s + duracoes[id]; }, 0);
      document.getElementById('ag6-cta-total').textContent = fmtT6Local(total);
      document.getElementById('ag6-cta-rot').textContent = n ? 'Escolher barbeiro' : 'Escolha um serviço';
      document.getElementById('ag6-cta-sub').textContent = n
        ? n + ' serviço' + (n > 1 ? 's' : '') + ' · ' + fmtDuracaoLocal(min)
        : 'para seguir pro barbeiro';
    }
    function validarSelecao() {
      return selecionados.size > 0;
    }
  </script>
<% } %>
